*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/
/app/release/

# NOTE: keystore/ and keystore.properties ARE committed on purpose so every
# CI build signs with the same key (see keystore.properties for the rationale).
